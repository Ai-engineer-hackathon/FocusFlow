// Optional: replace this shim with the real @mozilla/readability Readability.js file.
// Content script uses DOM-based extraction by default, but will use Readability if present.
// eslint-disable-next-line no-unused-vars
(() => {})();

